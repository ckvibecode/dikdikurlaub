services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dikdik
      POSTGRES_PASSWORD: dikdik
      POSTGRES_DB: dikdik
    ports:
      - '5432:5432'
    volumes:
      - dikdik_pgdata:/var/lib/postgresql/data

volumes:
  dikdik_pgdata:
